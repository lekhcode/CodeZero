/**
 * C++ prelude — gcc libstdc++ “single header” (LeetCode / Codeforces style).
 * User submission is inlined after prelude + nlohmann; still uses {@code Solution} + method from hints.
 */
export const CPP_PRELUDE = `#include <bits/stdc++.h>
using namespace std;

`.trimEnd();
